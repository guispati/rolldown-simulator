import styled from "styled-components";
import { ButtonContainer } from "../styles";
import hud from '../../../../../../../assets/hud.png';

export const UpgradeButtonContainer = styled(ButtonContainer)`
    background: url(${hud});
    background-position: -1268px -290px;    
`;